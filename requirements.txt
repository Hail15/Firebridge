# FireBridge — Python Dependencies
# Install: pip install -r requirements.txt

# Core API framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# HTTP client (NOAA API calls)
requests==2.32.3

# Geospatial processing (Phase 2 — polygon generation)
geopandas==0.14.4
shapely==2.0.4

# Map rendering (Phase 3 — Folium map output)
folium==0.16.0

# Form/file handling
python-multipart==0.0.9